{
  "name": "tradewise-ai",
  "version": "1.2.1",
  "description": "TradeWise AI - Intelligent Trading Journal with Behavioral Analytics",
  "private": true,
  "scripts": {
    "install:all": "cd backend && npm install && cd ../frontend && npm install",
    "verify-env": "cd backend && npm run verify-env"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
